package top.cookie;

import com.nukkitx.protocol.bedrock.BedrockServer;
import java.net.InetSocketAddress;

public class Server {
    public static void main(String[] args){
        
    }
    private static BedrockServer server = new BedrockServer(new InetSocketAddress("127.0.0.1",19132));
}
